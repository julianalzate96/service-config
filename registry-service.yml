server:
  port: 9091
eureka:
  instance:
    hostname: localhost
    client:
      registerWithEureka: false
      fetchRegistry: false
      serviceUrl:
        defaultZone: https://registry-service-cn.herokuapp.com//eureka/
